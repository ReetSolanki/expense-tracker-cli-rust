Expense-tracker in CLI in Rust

Basic commands such as add, list and delete for expenses
Fully runs locally, data stored in expenses.json

Entire app runs on the command line interface (CLI), just make sure to add the important guidance 
